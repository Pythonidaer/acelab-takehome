[project]
name = "acelab-takehome"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "openai>=1.59.0",
    "pydantic>=2.12.5",
    "python-dotenv>=1.0.0",
]

[project.scripts]
material-agent = "material_agent.__main__:main"

[project.optional-dependencies]
dev = [
    "ruff",
    "mypy",
    "pytest",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["acelab", "material_agent"]
